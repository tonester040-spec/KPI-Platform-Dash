# KPI Platform — Python dependencies
# Install: pip install -r requirements.txt

# Google APIs
google-auth>=2.27.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.116.0
gspread>=6.0.0

# OAuth 2.0 (Zenoti)
authlib>=1.3.0

# HTTP + resilience
requests>=2.31.0
backoff>=2.2.1

# Claude AI API
anthropic>=0.40.0

# Excel report generation
openpyxl>=3.1.2

# Image generation (PWA icons)
Pillow>=10.0.0

# Data validation
jsonschema>=4.21.0

# Environment + config
python-dotenv>=1.0.0

# Date/time utilities
python-dateutil>=2.9.0
pytz>=2024.1

# Tier 2 Excel parsers — .xls legacy format support
# Note: .xls → .xlsx conversion also requires LibreOffice installed on the runner:
#   sudo apt-get install -y libreoffice   (GitHub Actions / Ubuntu)
#   brew install --cask libreoffice       (macOS)
xlrd>=2.0.1
